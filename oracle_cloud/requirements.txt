# Minimal dependency set for the native (non-Docker) cloud deploy -- see
# DEPLOY.md. Same as airflow/requirements.txt minus vectorbtpro: the core
# pipeline (hourly_run.py, daily_run.py, and everything under extraction/)
# doesn't need it -- extraction/ohlcv.py's crypto fetch was ported from
# vectorbtpro's BinanceData wrapper to plain python-binance (already a
# dependency below) specifically so this deploy path doesn't need a private
# GitHub repo PAT. vectorbtpro is still used by research/strategy scripts
# outside this pipeline (fill_gaps.py, strategy/, pairs_trading/) and by
# utils.py::get_data_duckdb(asvbt=True) as an optional, lazily-imported
# convenience for that research code -- install it separately
# (`pip install -r airflow/requirements.txt` has it) only if you need those.
pandas>=2.3.3,<3
yfinance>=1.3.0
requests
tradingview-screener>=3.2.0
pandas-market-calendars>=5.3.2
alpaca-py>=0.43.5
duckdb>=1.5.2
# playwright is never launched on the cloud VM (WSB scraping stays local,
# see wsb_run.py) but validation.py imports extraction/wsb.py at module
# level for WSB_DB_PATH, which imports playwright.sync_api -- so it still
# needs to be *installed* for hourly_run.py/daily_run.py to import cleanly,
# even though its browser is never launched here. `playwright install
# chromium` itself is skippable on this deploy path (see DEPLOY.md) since
# nothing here actually launches a browser.
playwright>=1.59.0
praw>=7.8.1
prawcore>=2.4.0
python-dotenv>=1.2.2
vadersentiment>=3.3.2
tqdm
python-binance==1.0.36
